# Third-party code

Three.js **0.185.1**, copied from the corresponding npm package: MIT license. The complete notice is in `vendor/three/LICENSE`. Only the renderer, GLTF loader/exporter, BufferGeometryUtils and Meshopt decoder modules needed by this app are bundled.

The bundled Meshopt decoder identifies itself as meshoptimizer and carries its MIT notice in `vendor/three/examples/jsm/libs/meshopt_decoder.module.js`. Upstream: https://github.com/zeux/meshoptimizer.

Generic sample scene geometry and interface code were written for Scribble Space. No Pascal, openPlan3D, BCF or IIIF source code is incorporated; those projects/specifications are design references, not runtime dependencies.
